# Public URL of the FastAPI service (Railway backend).
# Required on the frontend service so the browser does not POST /api to itself (405).
# Example: https://backend-production-xxxx.up.railway.app
# Or Railway reference: https://${{backend.RAILWAY_PUBLIC_DOMAIN}}
VITE_API_BASE=
